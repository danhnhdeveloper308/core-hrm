# HRM — Tiến độ & bàn giao (đọc trước khi code)

> Mục đích: phiên Claude mới đọc file này + `CLAUDE.md` là nắm được hiện trạng, **không cần đọc toàn bộ code**. Cập nhật file này mỗi khi hoàn thành một mảng lớn.

Stack: NestJS 11 (`apps/api`) + Next.js 16 App Router (`apps/web`) + `@repo/shared` (zod là source of truth). Prisma 7 + Postgres, BullMQ + Redis, MinIO/S3 storage, Socket.IO, EventEmitter2. Chạy migrate: `printf 'y\n' | script -qec "sg docker -c 'pnpm db:migrate <ten>'" /dev/null` rồi `sg docker -c "pnpm --filter api exec prisma generate"`. Gate: `pnpm typecheck`, `pnpm lint`, `pnpm --filter api test` (54 unit), e2e: `cd apps/api && NODE_ENV=test pnpm exec jest --config test/jest-e2e.json --forceExit` (60 tests).

## ĐÃ XONG (Phase 0–8 phần lớn)

- **Phase 0–5**: auth (JWT cookie + refresh rotation + 2FA + recovery), RBAC (`resource:action`, `@RequirePermissions`), tổ chức (OrgUnit cây + materialized path, code unique theo cha), nhân viên, chấm công + bảng công (engine `apps/api/src/modules/attendance/timesheet.engine.ts` — clamp công vào [giờ ca, giờ tan]: `workStart=max(checkin,shiftStart)`, `workEnd=min(checkout,shiftEnd)`; firstIn/lastOut lưu giờ thực tế), face check-in (@vladmandic/human).
- **Phase 7 Nghỉ phép**: LeaveType (`requiresDocument` = cần giấy tờ), LeavePolicy (kế thừa theo cây), ledger (`LeaveBalanceEntry`, balance = SUM). Phép **không lương** → bỏ policy, không giới hạn, không trừ số dư. FE: `/dashboard/leave`, cấu hình `/dashboard/settings/leave`.
- **Phase 8 Approval engine N cấp** (`apps/api/src/modules/approval/`): `ApprovalFlow` + `ApprovalFlowStep` (5+1 approver type: DIRECT_MANAGER, MANAGEMENT_CHAIN(level), UNIT_MANAGER_OF_TYPE(theo loại đv, leo cây), **UNIT_MANAGER_OF_UNIT(chọn đúng 1 đv)**, ROLE, SPECIFIC_USER; mỗi step có `label` nhãn chữ ký + `slaHours` (thời hạn duyệt — MỚI CHỈ hiển thị, chưa nhắc/escalation; escalation để Phase 8 Notification)). Chọn flow theo priority + conditions JSON. Resolve approver snapshot tại submit, auto-skip bước rỗng/ra chính requester, OR trong 1 bước, HR override (`OVERRIDE_PERM`). `ApprovalInstance.summary` để inbox hiển thị nội dung mọi loại. Sự kiện `APPROVAL_DECIDED` (emitAsync, các module đích lắng nghe — leave/attendance-correction/OT/shift-batch). FE: `/dashboard/approvals` (inbox / đã xử lý / đơn của tôi), builder `/dashboard/settings/approval-flows`.
- **Đính kèm** (`apps/api/src/modules/attachment/`): model `Attachment` đa hình (targetType LEAVE_REQUEST/ATTENDANCE_CORRECTION/OT_REQUEST), ảnh/PDF ≤10MB qua StorageService, signed URL. FE component `AttachmentPicker`/`AttachmentList`.
- **Auth/User (1 cài đặt = 1 công ty)**: `User.email` nullable + `User.username` unique (=mã NV). Tạo employee LUÔN tạo user: có email → invite; không email → username + mật khẩu mặc định `Abcd123@`. Login bằng email HOẶC mã NV. Phone bắt buộc. Quên MK fallback: `POST /auth/reset-password-by-identity` (mã NV + SĐT). Xem memory `hrm-login-model`.
- **Điều chỉnh công**: `POST /attendance/corrections/request` → duyệt → áp log MANUAL + recalc. `GET /attendance/corrections/mine`.
- **OT cá nhân (phần mềm tự log)**: `OtRequest` (OVERTIME | SHIFT_SHIFT), `POST /attendance/ot/request` → duyệt → cộng otMinutes / dời giờ. FE ở `/dashboard/my-attendance`.
- **Phiếu tăng/giãn ca theo DANH SÁCH (nhà máy)**: `apps/api/src/modules/shift-registration/`. Upload Excel (exceljs, danh sách phẳng MSNV+ngày+loại+lý do) → `ShiftRegistrationBatch`+lines → luồng duyệt `SHIFT_BATCH` (chữ ký N cấp, nhãn cấu hình, cao nhất sát trái / Tổng hợp=người upload sát phải) → cấp cao nhất duyệt → `TimesheetService.applyShiftVariant` áp cho TẤT CẢ theo `OtCalcMode`. WorkShift có `gianCaEnd`/`tangCaEnd` + `otCalcMode` (override org). FE `/dashboard/shift-registrations` (tải mẫu, upload, danh sách, chi tiết + thống kê + chữ ký). Xem memory `hrm-timekeeping-flexibility`.
- **Tính công 2 chế độ** (`OtCalcMode`, org default + ca override): `CLAMP_TO_REGISTERED` (sản xuất — nới khung [giờ ca, mốc đăng ký], clamp); `SEPARATE_OT` (phần mềm — cộng otMinutes riêng).

### Roles (5) & visibility
- ORG_ROLES: ORG_ADMIN, HR_MANAGER, UNIT_MANAGER, **EMPLOYEE** (nhân viên văn phòng), **WORKER** (công nhân — cấp thấp nhất). Tạo employee KHÔNG email → mặc định role **WORKER** (`users.createEmployeeAccount`, fallback EMPLOYEE nếu org chưa seed WORKER); mời qua email → EMPLOYEE.
- Permission `SHIFT_REGISTRATION_MANAGE` (`shift_registration:manage`): có ở ORG_ADMIN/HR/UNIT_MANAGER/**EMPLOYEE**, KHÔNG có ở WORKER. Gate trang + endpoint `/shift-registrations` + nav. → EMPLOYEE trở lên xem/đăng ký phiếu + thống kê; WORKER không thấy.
- Thêm permission/role mới ⇒ `sg docker -c "pnpm db:seed"` (upsert Permission) + org mới tự seed qua `organizations.service`. **Org CŨ backfill bằng `pnpm db:sync-roles`** (tạo role thiếu vd WORKER + thêm permission mặc định còn thiếu, idempotent, KHÔNG xoá custom). Sau backfill user đang đăng nhập phải **đăng nhập lại** (cache permission).

### Quản lý role (roles.service)
- Platform admin (orgId=null, vd SUPER_ADMIN) thấy & sửa **MỌI role** (platform + tất cả org). Org admin chỉ role org mình. `RoleResponse` có `orgId`+`orgName` (FE hiện nhãn "Hệ thống" / tên org).
- ORG_ADMIN có ROLE_CREATE/UPDATE/DELETE → sửa permission role org mình (PUT `/roles/:id/permissions`, gate ROLE_UPDATE, không chặn isSystem trừ SUPER_ADMIN).
- Org admin KHÔNG được gán `PLATFORM_ONLY_PERMISSIONS` (org:create/delete) — chặn ở setPermissions + ẩn trong matrix FE.

### Approver "Quản lý đơn vị" + chainLevel
- UNIT_MANAGER_OF_TYPE (leo cây theo loại đv) và UNIT_MANAGER_OF_UNIT (chọn đúng 1 đv) đều nhận `chainLevel` tuỳ chọn: 1 = chính quản lý đơn vị, 2 = quản lý cấp trên của họ… (resolver `climbManager`). VD giám đốc TS1 = UNIT_MANAGER_OF_UNIT(TS1) chainLevel 1.

## Chấm công: geofence + chặn checkout muộn + kiosk public (2026-06-25)
- **Location (geofence)**: đã review — `haversineMeters` + `dist <= worksite.radiusM` đúng; flag `locationSuspect` khi accuracy>200m. Có unit test (boundary trong/ngoài bán kính).
- **Chặn chấm RA sau giờ tan**: `AttendanceService.assertCheckoutAllowed` — qua `shift.endTime` (ca ngày) → 422 `ATTENDANCE_AFTER_SHIFT_END`, KHÔNG tạo log OUT → ngày giữ "quên chấm ra" (IN không OUT) cho HR chấm bù (HR edit/correction sẵn có). Ca qua đêm/không ca → bỏ qua. Áp cho cả luồng cá nhân lẫn kiosk.
- **Kiosk public (nhận diện 1:N, không đăng nhập)**: `FaceService.identify` (so 1:N toàn org, antispoof gate, lưu ảnh). `AttendanceService.kioskCheck(worksiteId,...)` resolve org theo worksite → geofence → identify → `assertCheckoutAllowed` → createLog (source FACE). Endpoint `@Public` `GET /attendance/kiosk/:worksiteId` + `POST /attendance/kiosk/:worksiteId/check` (multipart, `@Throttle` 30/phút, `@Audit`). FE trang public `apps/web/src/app/kiosk/[worksiteId]/page.tsx` (camera + geo, hiển thị "Xin chào <tên>, đã chấm VÀO/RA"). proxy.ts KHÔNG chặn `/kiosk` → public. Trust model: thiết bị đặt tại worksite + antispoof + geofence + throttle.

## Cải thiện DB & fix (2026-06-25)
- **Bug bảng công T7/CN**: FE hardcode `wd===0||wd===6` → mọi T7/CN bị tô "ngày nghỉ" bất kể cấu hình ca. Đã sửa: `GET /attendance/grid` trả `{ restWeekdays, rows }` (`restWeekdays` resolve từ `workDays` của ca mặc định org/đơn vị, quy ước `getUTCDay()` 0=CN..6=T7); FE tô xám theo đó. **Lưu ý**: muốn header phản ánh T2–T7 thì ca `workDays=[1..6]` phải là ca mặc định org/đơn vị; ngày tính trước khi đổi cấu hình cần "Tính lại".
- **Soft-delete** Employee + Organization: thêm `deletedAt`. Xoá NV → set `deletedAt` + `status=TERMINATED` (tận dụng filter `status != TERMINATED` sẵn có); xoá Org → `deletedAt` + `SUSPENDED`. KHÔNG cascade xoá thật (giữ dữ liệu pháp lý). Unique `(orgId,code)` của Employee và `slug` của Org chuyển sang **PARTIAL UNIQUE `WHERE deletedAt IS NULL`** → tái dùng code/slug sau xoá. Filter `deletedAt: null` ở roster/findOne/me/requireEmployee + org list/requireOrg.
- **AuditLog.orgId**: thêm cột + index `[orgId, createdAt desc]`; interceptor populate `orgId` từ token; worker ghi + response có `orgId` → nền tảng cho audit theo tenant.
- **Chống duyệt trùng**: partial unique `ApprovalInstance(targetType,targetId) WHERE status='PENDING'` + guard `APPROVAL_ALREADY_PENDING` trong `createInstance`.
- Migration: `20260625023120_softdelete_auditorg_approval_uniq` (partial unique chèn tay vào file SQL — Prisma không diễn đạt được).

## Phase 8 Notification + Audit org-scope (2026-06-25)
- **Audit theo tenant**: `AuditLog.orgId` (đã thêm trước đó) nay được DÙNG: `AuditService.list(query, actorOrgId)` ép org user chỉ thấy log org mình; platform admin (orgId=null) thấy tất cả + lọc `?orgId=`. **ORG_ADMIN được cấp `AUDIT_READ`** (org cũ chạy `pnpm db:sync-roles` + đăng nhập lại). Socket audit tách room: platform → `room:audit`, org admin → `room:audit:{orgId}` (chống rò rỉ realtime chéo tenant).
- **Notification center 3 kênh (in-app + push + email)**: model `Notification` + `DeviceToken` (migration `20260625030320_phase8_notification`). `NotificationService.dispatch` → (1) row in-app + emit socket `notification:new`; (2) FCM multicast qua `NotificationQueueModule` (firebase-admin, **graceful** khi thiếu `FIREBASE_*`, worker prune token chết); (3) **email** qua `EmailQueueService.enqueueNotification` cho người nhận có email ACTIVE (`MailService.sendNotification`, opt-out qua `dispatch({ email: false })`). Endpoints `/notifications` (list cursor, unread-count, :id/read, read-all, device-tokens POST/DELETE) — cá nhân, `@SkipAudit`.
- **Triggers**: `ApprovalService.createInstance` → báo approver bước đầu (`APPROVAL_PENDING`); `decide` → báo approver bước kế hoặc báo requester kết quả (`APPROVAL_DECIDED`).
- **FE thông báo 2 lớp (như GitHub/FB)**: chuông `NotificationBell` (badge + dropdown + đọc 1/tất cả) + in-app toast (tab đang xem). Lớp OS-notification: `lib/fcm.ts` **tách quyền trình duyệt khỏi Firebase** — `pushSupported()` → Notification API bắn native khi user ở **tab khác** (`showNativeIfHidden`, KHÔNG cần Firebase); nếu có `NEXT_PUBLIC_FIREBASE_*` thì dùng FCM/service worker cho cả khi **đóng trình duyệt** (và tự tắt native để tránh trùng). Nút "Bật thông báo" trong chuông + refresh token khi đã cấp quyền.
- **Realtime 2 chiều**: socket `approval:changed` (event nội bộ `APPROVAL_CHANGED`) emit cho **requester + MỌI approver** khi phiếu đổi trạng thái → FE invalidate `['approval']` + domain theo `targetType` (`leave`/`attendance`/`shift-registrations`). Khắc phục: người gửi đơn trước đây chỉ thấy toast mà status không cập nhật (phải reload `/dashboard/leave`).
- **UX xin quyền không nag**: prompt gate theo `pushSupported()` (KHÔNG còn phụ thuộc Firebase — đây là lý do trước đây không hiện prompt khi chưa cấu hình FCM). Sau đăng nhập: đã cấp quyền → init im lặng; chưa cấp → `maybePromptForPush()` toast "Bật?" **1 lần/phiên**, "Để sau"/đóng → nhớ `localStorage` 7 ngày. `denied`/`unsupported` → không hỏi. Quyền chỉ xin trên user gesture (bấm "Bật").
- **SLA escalation/nhắc** (`ApprovalSlaService`, cron 30'): `ApprovalInstance.slaRemindedAt` (migration `20260625062103_approval_sla_reminded`). Bước hiện tại quá `slaHours` → nhắc người duyệt (`APPROVAL_PENDING` "quá hạn") + **escalate** quản lý cấp trên của họ (`GENERAL`). Nhắc 1 lần/bước (mốc `slaRemindedAt ≥ stepStart`), reset khi sang bước mới.
- **Tuỳ chọn nhận thông báo per-user (như GitHub)**: `User.notificationPrefs` JSON (migration `20260625070306_user_notification_prefs`) — ma trận **loại × kênh** (APPROVAL_PENDING/APPROVAL_DECIDED/GENERAL × inApp/email/push), mặc định bật hết (`resolveNotificationPrefs` ở `@repo/shared`). `dispatch` tôn trọng prefs từng người: chỉ tạo row/socket khi bật inApp, chỉ lấy token khi bật push, chỉ email khi bật email. Endpoints `GET/PUT /notifications/preferences`. FE `/dashboard/settings/notifications` (bảng switch, auto-save từng toggle, optimistic) + nav "Thông báo".
- **Huỷ đơn realtime**: `cancelByTarget` nay fetch instance → CANCELLED → báo người duyệt bước hiện tại (`GENERAL` "Đơn đã được người gửi huỷ", in-app, không email) + emit `approval:changed` (CANCELLED) cho requester + mọi approver → inbox gỡ item, dữ liệu cập nhật không reload. `history` ("Đã xử lý") nay GỒM cả đơn bị huỷ khi actor là người duyệt bước hiện tại → hiện trạng thái. Label CANCELLED đổi "Đã huỷ (người gửi)".
- E2e: `attendance-correction.e2e-spec.ts` xác thực pipeline (NV nộp → QL nhận PENDING → duyệt → NV nhận DECIDED → read-all → unread 0 → đăng ký token) + SLA (quá hạn → nhắc 1 lần) + preferences (tắt email → có in-app, không email) + huỷ đơn (CANCELLED hiện ở "Đã xử lý" của QL).

## Phase 9 (phần 1) + Hồ sơ NV mở rộng + Avatar (2026-06-25)
- **Avatar tự cập nhật**: `User.avatarKey` (migration `20260625072241_...`). `POST /users/me/avatar` (multipart) → storage + set avatarKey; `auth.me` ký signed URL tươi (24h, ưu tiên hơn avatarUrl OAuth). FE: card đổi ảnh ở `/dashboard/profile`.
- **Hồ sơ nhân sự theo pháp luật VN**: thêm trường Employee (CCCD + ngày/nơi cấp, MST, số sổ BHXH, số thẻ BHYT, ngân hàng, địa chỉ thường trú/tạm trú, liên hệ khẩn cấp, hôn nhân `MaritalStatus`, dân tộc/quốc tịch/tôn giáo, trình độ/chuyên ngành, email cá nhân) + model **`Dependent`** (người phụ thuộc — giảm trừ gia cảnh). CRUD dependent: `/employees/:id/dependents`. FE form NV mở rộng theo nhóm + quản lý người phụ thuộc (edit mode). `findOne`/`me` trả `dependents`.
- **Phase 9 Dashboard**: `GET /reports/dashboard` (REPORT_READ) → số liệu org (NV đang làm, có mặt/đi trễ/vắng/nghỉ hôm nay, đơn chờ duyệt, đơn nghỉ chờ). FE card ở `/dashboard` (gate REPORT_READ).
- **Phase 9 Reports**: `GET /reports/attendance.xlsx?from=&to=&orgUnitId=` (exceljs, đồng bộ stream) — tổng hợp công/NV (đủ công/trễ/sớm/vắng/nghỉ/lễ/giờ công/giờ OT). FE nút "Xuất Excel" ở bảng công. (BullMQ async/báo cáo theo-đơn-vị nhà máy để Phase 9 phần 2.)
- E2e: employees (hồ sơ VN + CRUD người phụ thuộc).
- **Employee detail sheet** hiển thị đầy đủ trường hồ sơ mới (giấy tờ/bảo hiểm/ngân hàng/địa chỉ/liên hệ khẩn cấp/khác) + danh sách người phụ thuộc (section tự ẩn khi rỗng).
- **Deploy**: [`DEPLOYMENT.md`](DEPLOYMENT.md) (Neon + Vercel FE + BE host) + env mẫu [`apps/api/env.production.example`](apps/api/env.production.example), [`apps/web/env.production.example`](apps/web/env.production.example). File cấu hình sẵn: [`railway.json`](railway.json), [`render.yaml`](render.yaml) (BE), [`apps/web/vercel.json`](apps/web/vercel.json) (FE — build command monorepo). Tinh chỉnh: API listen theo `PORT` nền tảng; `REDIS_TLS` (Upstash).
- **Cookie cross-domain**: env `COOKIE_SAMESITE` (lax|strict|none). FE/BE KHÁC domain (Vercel + Railway/Render) → `none` + `COOKIE_DOMAIN` trống (Secure tự bật). Cùng site → `lax`. `cookies.ts` đọc config; CORS đã `credentials:true` + origin chính xác.
- **Fix 2FA bypass OAuth**: `googleCallback` nay check `totpEnabled` → nếu bật, KHÔNG cấp session mà trả `pending2faToken`; controller redirect `…/login?pending2fa=…`; FE login đọc query → vào bước nhập mã 2FA (tái dùng `/auth/2fa/verify`). Trước đây login Google bỏ qua 2FA (lỗ hổng).

## CHƯA LÀM (roadmap còn lại)

1. **Phase 8 — tinh chỉnh (tuỳ chọn)**: email digest (gộp nhiều sự kiện) thay vì gửi mỗi lần. FCM push khi đóng trình duyệt cần `FIREBASE_*` + `NEXT_PUBLIC_FIREBASE_*` (xem `.env.example`).
2. **Phase 9 phần 2**: báo cáo tổng hợp theo ĐƠN VỊ kiểu nhà máy (LĐ có mặt / xuống ca / giãn ca / tăng ca) + export qua BullMQ (async, file lớn) + thêm biểu đồ dashboard.
3. **Phase 10 Hardening**: perf, checklist nghiệm thu.
4. **Phase 6 Máy vân tay ZKTeco/iclock** (LÀM CUỐI — sau khi user cài phần cứng): ingest log → `AttendanceService.createLog` (idempotent) → recalc.

### ⚠️ Lỗi tồn đọng đã biết
- **Ca qua đêm**: `timesheet.engine.ts` so phút-trong-ngày, ca `endTime < startTime` (vd 22:00–06:00) cho `workMinutes=0` (SAI). Hiện CHỈ dùng ca trong ngày nên tạm bỏ qua (user xác nhận 2026-06-25). Khi cần ca đêm: xử lý `shiftEnd += 1440` khi vắt nửa đêm + mở rộng cửa sổ lấy log sang ngày kế (`localDayRangeUtc`).

## Quy ước quan trọng (đừng phá)
- zod schema ở `@repo/shared` là source of truth; DTO backend = `createZodDto`; build shared trước khi typecheck api (`pnpm --filter @repo/shared build`).
- Mọi mutation: `@RequirePermissions` + `@Audit`. Lỗi: `AppException` + `ERROR_CODES`.
- FE: fetch wrapper `apps/web/src/lib/api/client.ts` (không axios); form RHF + zodResolver pattern 3-generic khi schema có `.default()`/`.transform()`; query keys tập trung `lib/api/query-keys.ts`.
- Commit theo từng cụm, chạy gate trước khi báo xong. Migrate cần `sg docker` + regenerate prisma client.
